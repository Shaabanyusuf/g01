def add (a,b)
 return(a+b)
 
 def sub (a,b)
  return(a-b)
 
 
 def mult (a,b)
  return(a*b)
 
 def dev (a/b)
 if b==0:
 	return none
  return(a/b)
 
 print (add(2.4))
 print (sub(2.4))
 print (mult(2.4))
 print (dev(2.4))
 
 
 
 
